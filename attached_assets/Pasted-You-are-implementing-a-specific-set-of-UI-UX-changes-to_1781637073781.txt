You are implementing a specific set of UI/UX changes to the Sri Velmayil Jewellery 
Next.js website. This is a surgical update — you modify ONLY what is listed below. 
Nothing else is touched. No refactoring, no "improvements", no restructuring.

=============================================================
SECTION 0: PRE-IMPLEMENTATION AUDIT
=============================================================

Before writing a single line of code, read and fully understand:

- components/Navbar.tsx (or wherever the navigation bar is defined)
- app/page.tsx (or app/(home)/page.tsx — the homepage)
- Any component files imported into the homepage (hero, collections, FAQ, footer, etc.)
- components/ directory — scan all files to map what already exists
- The existing FAQ component/section (wherever it currently lives)
- The existing Collections section on the homepage
- The existing hero/banner section on the homepage
- Any ticker or marquee component that may already exist

Map out before touching anything:
1. The exact file and line where each nav item is defined
2. The exact file and component for the hero section
3. The exact file and component for the Collections section
4. Where the "Plan Your Purchase" section lives
5. Where the FAQ section currently lives (nav + page)
6. Where the footer is defined (you will insert FAQ above it)
7. Whether a ticker/marquee component already exists anywhere

=============================================================
SECTION 1: NAVIGATION BAR CHANGES
=============================================================

File: components/Navbar.tsx (or equivalent)

--- LOGO ---
Update the website logo to use the latest logo asset.
If the logo image path is the same file being replaced externally, just ensure 
the <img> or <Image> src is pointing to the correct updated logo path.
Do not change logo sizing, positioning, or styling unless it breaks layout.

--- REMOVE THESE NAV ITEMS ENTIRELY ---
Remove the following navigation links. Delete both the JSX element and any 
associated routing logic, active-state logic, or mobile menu entries for each:

1. Silver Rate
2. Rate History
3. Blog
4. FAQ
5. Admin

Remove them from:
- Desktop nav menu
- Mobile/hamburger menu (if separate)
- Any navigation config arrays or constants files that define nav items

--- FINAL NAV ORDER ---
After removals, the navigation must appear in this exact order:

1. Logo (leftmost, not a nav link — it's the brand mark)
2. Home
3. About Us
4. Collections
5. Today's Rate
6. Contact Us
7. Call (CTA button — keep its existing style, just ensure correct position)

Reorder the nav items to match this sequence exactly.
Do not rename any items. Do not change their existing href/routing.
Do not change any styling of the nav items themselves.

=============================================================
SECTION 2: HOMEPAGE — LIVE GOLD RATE TICKER
=============================================================

Add a live gold rate ticker at the very top of the homepage, above everything 
else including the navbar (or immediately below the navbar if above causes 
layout issues with sticky/fixed positioning — use your judgment based on the 
existing layout).

Ticker requirements:
- Continuously scrolling marquee/ticker animation (CSS marquee or JS scroll loop)
- Displays current gold rate data fetched from the existing rates source 
  (reuse whatever hook, API call, or utility already fetches rates — 
  do NOT create a new fetching mechanism)
- Display format in the ticker:
  "✦ 22K Gold: ₹[rate]/gm  |  24K Gold: ₹[rate]/gm  |  Silver: ₹[rate]/gm  ✦"
  Repeat the content to create a seamless loop
- Styling: gold/amber color scheme consistent with the site's luxury theme
  Dark background bar (use the site's existing dark color tokens)
  Small, clean text — not oversized
- The ticker must NOT cause layout shift or overflow issues
- If rates are loading, show: "✦ Fetching live rates..." in the ticker
- If rates fail, show: "✦ Live rates unavailable — please call us" in the ticker

Implementation approach:
- Create a new component: components/GoldRateTicker.tsx
- Use CSS animation (animation: marquee linear infinite) — no external libraries
- Import and place it at the top of the homepage layout

=============================================================
SECTION 3: HOMEPAGE — HERO SECTION REDESIGN (SLIDESHOW)
=============================================================

Replace the existing static hero section with a slideshow/carousel.

Slideshow requirements:
- Auto-advances every 4–5 seconds
- Smooth fade or slide transition between images
- Shows 3–5 gold-themed banner slides
- Each slide is a full-width hero banner (same dimensions as the current hero)
- Navigation dots at the bottom of the slideshow
- Previous/Next arrow controls on the sides
- Pause on hover (optional but preferred)

Image sources for the slides:
- Use existing hero/banner images already in the project's public/ or assets/ folder
- If only one hero image exists, duplicate it across slides as placeholders 
  and add a comment: // TODO: Replace with actual banner images
- Do NOT fetch external images or use placeholder services

Implementation approach:
- Do NOT install any carousel library (no swiper, no embla, no slick)
- Implement using React useState + useEffect with CSS transitions only
- Create a self-contained component: components/HeroSlideshow.tsx
- Replace the existing hero component usage in app/page.tsx with <HeroSlideshow />
- Preserve all existing hero text overlays (tagline, CTA button) and apply 
  them consistently across all slides

=============================================================
SECTION 4: HOMEPAGE — REMOVE "PLAN YOUR PURCHASE" SECTION
=============================================================

Locate the "Plan Your Purchase" section on the homepage.
Delete it entirely — the JSX, the component file if it's standalone, 
and the import statement in the homepage.

After deletion, verify there is no empty whitespace gap left where it was.
The sections above and below it should flow together naturally.

=============================================================
SECTION 5: HOMEPAGE — COLLECTIONS SECTION UPDATE
=============================================================

Locate the existing Collections section on the homepage.

Update it to prominently display exactly two category boxes:
1. Gold
2. Silver

Requirements:
- Two large, visually prominent category boxes side by side (or stacked on mobile)
- Each box should have:
  - A category label ("Gold Collections" / "Silver Collections")
  - A background image or gold/silver themed gradient
  - A hover effect (scale, brightness, or overlay — match existing site style)
  - A link/button to the relevant collections page or filter
- Remove any other category boxes that currently exist (if there are more than 2)
- Keep the section heading ("Our Collections" or whatever it currently says)
- Match the existing card/box styling pattern used elsewhere on the site

Do NOT redesign from scratch — adapt what already exists.

=============================================================
SECTION 6: HOMEPAGE — ADD FAQ SECTION ABOVE FOOTER
=============================================================

The FAQ section currently exists somewhere in the codebase 
(either as a standalone page or component).

Step 1: Find it. Read it completely.

Step 2: Extract or reuse the FAQ component so it can be placed on the homepage.
- If it's already a reusable component, import it directly.
- If it's embedded in a page, extract it into components/FAQ.tsx first, 
  then import it into both its original location (if still needed) and the homepage.

Step 3: Place the FAQ component on the homepage, directly above the <Footer /> component.
The homepage section order from top to bottom must be:

  1. GoldRateTicker (new)
  2. Navbar
  3. HeroSlideshow (updated)
  4. [remaining existing homepage sections in their current order]
  5. Collections (updated — Gold + Silver boxes)
  6. [any other existing sections]
  7. FAQ (added here)
  8. Footer

Step 4: Style the FAQ section to match the homepage's luxury theme.
- If it has its own styling, keep it.
- Ensure it doesn't look out of place between the last content section and the footer.

=============================================================
SECTION 7: REMOVE FAQ FROM NAVIGATION
=============================================================

This is already covered in Section 1 (removing FAQ nav item).
Double-check it is removed from:
- Desktop nav
- Mobile nav
- Any nav config/constants files
- Any sitemap or metadata navigation references

=============================================================
SECTION 8: STRICT SCOPE — DO NOT TOUCH
=============================================================

Do NOT modify any of the following:
- Any admin panel pages or components
- Poster Studio (components/PosterStudio.tsx)
- Any API routes
- Authentication / middleware / session logic
- Rate fetching utilities (lib/live-rates.ts, utils/rates.ts)
- Today's Rate page content
- About Us page content
- Collections page content
- Contact Us page content
- Footer content or styling
- Any existing SEO metadata, schema tags, or OpenGraph tags
- Cloudinary integration
- Any environment variables

=============================================================
SECTION 9: RESPONSIVE DESIGN RULES
=============================================================

Every change must work correctly at these breakpoints:
- Mobile: 375px–767px
- Tablet: 768px–1023px
- Desktop: 1024px+

Specific rules:
- Ticker: full width at all breakpoints, no overflow
- HeroSlideshow: full width, correct aspect ratio maintained at all sizes
- Collections boxes: side-by-side on tablet/desktop, stacked on mobile
- FAQ section: readable padding and font sizes at all breakpoints
- Nav changes: verify mobile hamburger menu also reflects all nav removals

=============================================================
SECTION 10: FINAL VERIFICATION CHECKLIST
=============================================================

Verify every item before marking complete:

Navigation:
[ ] Logo updated
[ ] Silver Rate nav item removed (desktop + mobile)
[ ] Rate History nav item removed (desktop + mobile)
[ ] Blog nav item removed (desktop + mobile)
[ ] FAQ nav item removed (desktop + mobile)
[ ] Admin nav item removed (desktop + mobile)
[ ] Nav order matches: Logo → Home → About Us → Collections → Today's Rate → Contact Us → Call
[ ] No broken links in remaining nav items

Homepage:
[ ] Gold rate ticker appears at top, scrolls continuously
[ ] Ticker shows live rate data when loaded, fallback text when loading/failed
[ ] Hero section is now a slideshow with auto-advance and navigation dots
[ ] "Plan Your Purchase" section is completely gone with no whitespace gap
[ ] Collections section shows Gold and Silver category boxes only
[ ] FAQ section appears directly above the footer
[ ] Homepage section order is correct top to bottom

General:
[ ] All changes are responsive — tested at mobile, tablet, desktop widths
[ ] No TypeScript errors (run tsc --noEmit)
[ ] No console errors on page load
[ ] No existing functionality broken
[ ] No unintended files modified outside this scope

=============================================================
SECTION 11: FILES YOU WILL CREATE
=============================================================

CREATE (new files only):
- components/GoldRateTicker.tsx

CREATE ONLY IF FAQ is not already a standalone component:
- components/FAQ.tsx (extracted from existing page)

CREATE:
- components/HeroSlideshow.tsx

MODIFY (only these):
- components/Navbar.tsx (or equivalent nav file)
- app/page.tsx (or homepage file)
- Any nav config/constants file where nav items are defined
- components/FAQ.tsx source location if extraction is needed

DO NOT create any other files.
DO NOT install any new npm packages.
DO NOT restructure the project.

=============================================================
BEGIN. Read all relevant files first. Implement section by section in order.
=============================================================