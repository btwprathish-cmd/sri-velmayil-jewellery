{
  "framework": null,
  "installCommand": "corepack enable pnpm && pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/sabarish run build",
  "outputDirectory": "artifacts/sabarish/dist/public",
  "rewrites": [
    { "source": "/api/rates/history", "destination": "/api/rates/history" },
    { "source": "/api/rates/latest",  "destination": "/api/rates/latest" },
    { "source": "/api/rates/:date",   "destination": "/api/rates/[date]" },
    { "source": "/((?!api/).*)",      "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        { "key": "Access-Control-Allow-Origin",  "value": "*" },
        { "key": "Access-Control-Allow-Methods", "value": "GET,OPTIONS" }
      ]
    },
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options",        "value": "DENY" }
      ]
    }
  ]
}
