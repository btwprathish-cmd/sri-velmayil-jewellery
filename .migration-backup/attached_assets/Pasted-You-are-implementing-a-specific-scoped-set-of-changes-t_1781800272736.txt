You are implementing a specific, scoped set of changes to the Sri Velmayil Jewellery Next.js website. This is a surgical update. You modify ONLY what is listed. No refactoring, no unsolicited redesigns, no restructuring of anything outside scope.

=============================================================
SECTION 0: PRE-IMPLEMENTATION AUDIT
=============================================================

Before writing a single line of code, read and fully understand:

- components/Navbar.tsx (or equivalent nav file)
- app/collections/page.tsx (or wherever collections are rendered)
- app/collections/ directory — all files and subdirectories
- components/ directory — scan everything, map all existing components
- app/todays-rate/page.tsx (or equivalent)
- app/contact/page.tsx (or equivalent)
- lib/live-rates.ts
- utils/rates.ts
- Any admin panel route files under app/api/admin/ or app/admin/
- components/AdminLoginForm.tsx (or equivalent)
- Any existing category or product data structures (JSON files, DB queries, or hardcoded arrays)
- Any place in the codebase where collections are displayed (search for "Collections", "collections", "Gold", "Silver" across all component files)

Map out before touching anything:
1. How collections are currently structured (flat list? nested? categories?)
2. Where the Admin Login link currently lives in the nav
3. What the Today's Rate page currently contains — every section
4. What the Contact Us page currently contains — every section
5. Where rates are currently fetched from and what APIs are used
6. The exact function/hook names used for fetching rates
7. Where the "Estimate Gold Ornament Price" section is defined
8. Any shared layout components that render collections across multiple pages

=============================================================
SECTION 1: COLLECTIONS — MOVE ADMIN ACCESS
=============================================================

File: components/Navbar.tsx (or equivalent)

REMOVE the Admin Login nav item from the navigation bar entirely.
- Remove from desktop nav
- Remove from mobile/hamburger menu
- Remove from any nav config arrays or constants

File: app/collections/page.tsx (or equivalent Collections page)

ADD a "+" icon button at the top-right corner of the Collections page.

Requirements:
- Position: absolute or relative top-right of the Collections page header area
- Icon: use a "+" (plus) character or the existing Lucide React PlusCircle/Plus icon 
  (check which icon library is already used in the project and use that)
- Styling: subtle, premium — do not make it look like a random floating button
  Match the existing luxury dark theme of the site
  Small but visible — approximately 36x36px touch target
  Use a muted gold or white icon color with hover state (slight brightness increase)
- On click: open the Admin Login modal/form (reuse the existing AdminLoginForm component exactly as-is)
  If AdminLoginForm is currently shown as a full page, convert it to render inside a modal overlay
  Modal: dark overlay background, centered card, close button (×) at top-right of modal
  Pressing Escape or clicking outside the modal closes it
- Do NOT change anything about AdminLoginForm itself — only how it is presented (modal wrapper)

State to add to Collections page:
const [showAdminLogin, setShowAdminLogin] = useState(false);

=============================================================
SECTION 2: COLLECTIONS — RESTRUCTURE HIERARCHY
=============================================================

The new collections structure must follow this exact hierarchy:

Level 1: Metal Type    → Gold | Silver
Level 2: Category      → Coin | Ring | Chain | Earring | Bracelet | Anklet
Level 3: Products      → items uploaded via Admin Panel

--- ROUTING STRUCTURE ---

Implement the following URL structure:

/collections                          → shows Gold and Silver tiles
/collections/[metal]                  → e.g. /collections/gold shows 6 category tiles
/collections/[metal]/[category]       → e.g. /collections/gold/ring shows products

Use Next.js App Router dynamic segments:
- app/collections/page.tsx            → Metal selector (Gold / Silver)
- app/collections/[metal]/page.tsx    → Category selector
- app/collections/[metal]/[category]/page.tsx → Products grid

--- LEVEL 1: METAL SELECTOR PAGE (app/collections/page.tsx) ---

Display exactly two tiles:
- Gold Collections
- Silver Collections

Each tile:
- Large, visually prominent card
- Gold-themed gradient or gold image background for Gold tile
- Silver-themed gradient or silver image background for Silver tile
- Hover: subtle scale(1.02) or brightness increase
- Clicking navigates to /collections/gold or /collections/silver
- Consistent with existing site card styling

Keep the "+" Admin Login icon at top-right of this page (from Section 1).

--- LEVEL 2: CATEGORY PAGE (app/collections/[metal]/page.tsx) ---

Display exactly 6 category tiles for whichever metal was selected:

1. Coin
2. Ring
3. Chain
4. Earring
5. Bracelet
6. Anklet

Each tile:
- Category name displayed prominently
- Appropriate icon or placeholder image
- Clicking navigates to /collections/[metal]/[category]
- Breadcrumb at top: Collections > [Metal] (clickable back navigation)
- Consistent card styling with Level 1

--- LEVEL 3: PRODUCTS PAGE (app/collections/[metal]/[category]/page.tsx) ---

Display products uploaded via the Admin Panel for the selected metal + category combination.

Requirements:
- Breadcrumb: Collections > [Metal] > [Category]
- Product grid: 2 columns mobile, 3 columns tablet, 4 columns desktop
- Each product card shows:
  - Product image (from Cloudinary or local storage — reuse existing image display logic)
  - Product name
  - Making charge / price if available
  - Existing card styling from the current collections implementation
- If no products exist for this metal + category: show a clean empty state
  "No [Category] available in [Metal] collection yet."
  Do not show an error — just a tasteful empty message
- Fetch products by filtering on metal type AND category
  Reuse the existing collections data fetching mechanism (API route, JSON, etc.)
  Add metal and category as filter parameters to whatever data source already exists

--- ADMIN PANEL PRODUCT UPLOAD ---

In the existing Admin Panel collections management:
- Add a "Metal Type" field (dropdown: Gold | Silver) to the product upload form
- Add a "Category" field (dropdown: Coin | Ring | Chain | Earring | Bracelet | Anklet)
- These two fields must be saved alongside the existing product data
- Read the existing collections data structure first — extend it, do not replace it
- Existing products without metal/category should default to metal: "Gold", category: "Ring"
  Add a comment: // TODO: Admin should re-categorize legacy products

--- CONSISTENCY ENFORCEMENT ---

Search the entire codebase for every place collections are displayed:
- Homepage Collections section
- Any "featured collections" or "preview collections" component
- Any other page referencing collections

Update ALL of them to:
- Link to /collections (not to a flat collections page)
- Use the same Gold / Silver first-level structure
- Never show a flat uncategorized product list as the entry point

=============================================================
SECTION 3: TODAY'S RATE PAGE — LAYOUT + CONTENT
=============================================================

File: app/todays-rate/page.tsx (or equivalent)

--- REMOVE ---

Locate and completely remove the "Estimate Gold Ornament Price" section:
- Delete the JSX block
- Delete the standalone component file if it exists
- Remove the import statement
- Verify no empty gap is left — surrounding sections flow cleanly together

--- LAYOUT FIXES ---

Read the current layout carefully. Fix the following:

1. Rate Cards Equal Height:
   All rate information cards must be the same height.
   Use CSS: align-items: stretch on the grid/flex container
   Each card must use h-full on its inner content wrapper
   No card should appear taller than another due to content differences

2. Card Consistent Sizing and Spacing:
   All cards must have identical:
   - Padding (use the same Tailwind padding classes across all cards)
   - Border radius
   - Border style
   - Shadow
   If cards currently have inconsistent padding or spacing, normalize them to 
   a single standard (read what the most common current value is and apply it everywhere)

3. Informational/Hint Section Alignment:
   The hint/info section below the rate cards must:
   - Match the max-width of the cards section above it
   - Have identical horizontal padding/margin as the cards container
   - Not appear misaligned or offset relative to the cards
   Wrap both the cards section and the hint section in a single container div 
   with a consistent max-width and mx-auto centering if not already done

4. Section Headings Alignment:
   All section headings on the page must be consistently aligned (left-aligned 
   or center-aligned — match whatever the current dominant pattern is)

5. General Professional Alignment:
   Review the full page. Any element that appears visually off-center, 
   misaligned, or inconsistently spaced relative to its siblings — fix it.
   Do not redesign — align.

=============================================================
SECTION 4: CONTACT US PAGE — LAYOUT REFINEMENT
=============================================================

File: app/contact/page.tsx (or equivalent)

Read the current Contact Us page layout completely.

Fix the following without redesigning:

1. Section Spacing:
   All sections must have consistent vertical spacing between them.
   Normalize to whatever spacing value is most common on the page currently.

2. Form Alignment (if a contact form exists):
   - All form labels must be consistently aligned
   - All input fields must be the same width within their container
   - Submit button must be properly sized and not overflow its container

3. Info Blocks (address, phone, hours if displayed):
   - All info blocks must be the same width and padding
   - Icons (if used) must be vertically aligned with their text
   - Text must not overflow or wrap awkwardly at any breakpoint

4. Map Embed (if exists):
   - Must be full-width within its container
   - Must have consistent margin/padding above and below
   - Must not overflow on mobile

5. Responsive Review:
   - At 375px: no horizontal overflow, all text readable, no elements touching screen edges
   - At 768px: two-column layouts (if any) must align properly
   - At 1024px+: page must not look too narrow or too stretched

Do not change any content, copy, phone numbers, addresses, or links.
Only fix layout, spacing, and alignment.

=============================================================
SECTION 5: LIVE GOLD & SILVER RATES — DATA ACCURACY
=============================================================

Files: lib/live-rates.ts, utils/rates.ts (and related)

Read the current implementation completely before making any changes.

Requirements:

1. Tamil Nadu Market Standards:
   Rates must reflect Tamil Nadu, India local market rates.
   
   The correct calculation for Tamil Nadu:
   - Source: International gold spot price (USD per troy oz)
   - Convert: USD → INR using live forex rate (reuse existing Frankfurter API call)
   - Convert: Troy oz → grams (1 troy oz = 31.1035 grams)
   - Apply 22K purity: price_per_gram * (22/24) = 22K rate
   - Apply 24K purity: price_per_gram = 24K rate  
   - Apply 18K purity: price_per_gram * (18/24) = 18K rate
   - Tamil Nadu typically adds 1–3% above the base calculated rate due to 
     local market premiums — add a configurable LOCAL_PREMIUM_PERCENT constant 
     (default: 1.5) that can be adjusted without code changes
   - Silver: convert silver spot price to INR per gram using same forex rate

2. API Waterfall (keep existing structure, just verify it's correct):
   Primary: GoldAPI.io (existing GOLD_API_KEY env var)
   Fallback: Metalprice API (existing METALPRICE_API_KEY env var)
   Forex: Frankfurter API (free, no key needed)
   
   If primary fails → use fallback
   If both fail → return last cached value with a stale: true flag
   If no cache exists → return error

3. Cache:
   Cache duration: 30 minutes (keep existing if already 30 min)
   Cache storage: wherever it currently stores (do not change storage mechanism)
   
4. Displayed Rate Verification:
   Add a console.log in development only (process.env.NODE_ENV === 'development'):
   console.log('[Rates]', { source, gold22k, gold24k, silver, fetchedAt, localPremium })
   This helps verify rates are correct during testing.

5. Do NOT change:
   - The function signatures that other parts of the codebase call
   - The return data shape (only ADD localPremium to the returned object if not present)
   - The caching mechanism
   - Any API route that calls these utilities

Add this constant near the top of lib/live-rates.ts:
const LOCAL_PREMIUM_PERCENT = 1.5; // Tamil Nadu local market premium — adjust as needed

Apply it: finalRate = calculatedRate * (1 + LOCAL_PREMIUM_PERCENT / 100)

=============================================================
SECTION 6: RESPONSIVE DESIGN REVIEW
=============================================================

After implementing all sections above, perform a responsive review pass
on every page and component you have modified.

For each modified page/component, verify at these breakpoints:
- 375px (mobile)
- 768px (tablet)
- 1280px (desktop)

Check for and fix:
- Horizontal overflow (anything causing a scrollbar on the x-axis)
- Elements touching the screen edge without padding on mobile
- Text that wraps awkwardly or becomes unreadable
- Images that overflow their containers
- Flex/grid layouts that break at smaller sizes
- Buttons or interactive elements with insufficient touch target size (<44px)
- Any z-index issues causing elements to overlap incorrectly
- The new collections hierarchy pages at all sizes
- The Admin Login modal on mobile (must be full-screen or near-full-screen on 375px)

Fix every issue found. Do not leave known responsive bugs.

=============================================================
SECTION 7: SITE-WIDE AUDIT
=============================================================

After all above changes are implemented, perform a targeted audit.

Check only files you have modified or that depend on modified components.

Look for and fix:
1. Broken imports — any import pointing to a file that no longer exists or moved
2. Unused imports — clean up any imports made orphaned by your changes
3. Console errors — any error that would appear in browser console on page load
4. TypeScript errors — run tsc --noEmit and fix every error
5. Missing keys on mapped lists — any .map() without a key prop
6. Hydration mismatches — any component using browser APIs without 'use client' 
   directive or without mounted state guards
7. Broken navigation links — any <Link> or <a> pointing to routes that no longer exist
   (e.g. old flat /collections links should now point to /collections)
8. Empty catch blocks — any catch block that silently swallows errors without 
   at least a console.error

Do NOT audit or touch files you have not modified.

=============================================================
SECTION 8: STRICT SCOPE — DO NOT TOUCH
=============================================================

Do NOT modify any of the following unless a specific section above requires it:

- middleware.ts
- utils/auth.ts
- utils/auth-edge.ts
- utils/poster-engine.ts
- components/PosterStudio.tsx
- Any existing API routes not mentioned above
- Footer content or structure
- Homepage (except the Collections section link which must point to /collections)
- About Us page
- Any SEO metadata, schema tags, or OpenGraph tags
- Cloudinary upload integration
- Environment variables (do not add, remove, or rename any)
- Any authentication or session logic

=============================================================
SECTION 9: FILES TO CREATE / MODIFY
=============================================================

CREATE (new files):
- app/collections/[metal]/page.tsx
- app/collections/[metal]/[category]/page.tsx

MODIFY (existing files — only these):
- components/Navbar.tsx — remove Admin nav item
- app/collections/page.tsx — add "+" icon, Admin modal, Gold/Silver tiles
- app/todays-rate/page.tsx — layout fixes, remove Estimate section
- app/contact/page.tsx — layout refinement
- lib/live-rates.ts — LOCAL_PREMIUM_PERCENT, Tamil Nadu accuracy
- Admin panel product form — add Metal Type + Category fields
- Any collections data route (add metal + category filter support)
- Any homepage Collections section (update links to /collections)

DO NOT create any other files.
DO NOT install any new npm packages.
DO NOT restructure the project folder layout.

=============================================================
SECTION 10: FINAL VERIFICATION CHECKLIST
=============================================================

Collections:
[ ] Admin Login removed from navbar (desktop + mobile)
[ ] "+" icon appears at top-right of Collections page
[ ] Clicking "+" opens Admin Login modal
[ ] Modal closes on × click, Escape key, and outside click
[ ] /collections shows Gold and Silver tiles
[ ] /collections/gold shows 6 category tiles (Coin, Ring, Chain, Earring, Bracelet, Anklet)
[ ] /collections/silver shows the same 6 category tiles
[ ] /collections/gold/ring shows products (or clean empty state)
[ ] Breadcrumb navigation works at Level 2 and Level 3
[ ] Admin Panel product upload form has Metal Type + Category fields
[ ] Homepage Collections section links to /collections
[ ] All other collections references across the site use the new hierarchy

Today's Rate:
[ ] "Estimate Gold Ornament Price" section completely removed
[ ] All rate cards are equal height
[ ] Cards have consistent padding, border, shadow
[ ] Hint/info section aligns with cards container
[ ] No layout issues at mobile, tablet, desktop

Contact Us:
[ ] All sections have consistent spacing
[ ] Form fields (if any) are consistently aligned
[ ] Info blocks are visually balanced
[ ] No layout issues at 375px, 768px, 1280px

Live Rates:
[ ] LOCAL_PREMIUM_PERCENT constant added and applied
[ ] Tamil Nadu calculation formula is correct (spot → INR → per gram → purity → premium)
[ ] API waterfall still works (primary → fallback → cache)
[ ] Dev console.log added for rate verification
[ ] No existing rate function signatures broken

General:
[ ] tsc --noEmit passes with zero errors
[ ] No console errors on any page load
[ ] No horizontal overflow on any page at 375px
[ ] No broken navigation links anywhere on the site
[ ] All existing features continue to work as before

=============================================================
BEGIN. Read all relevant files first. Implement section by section in order.
=============================================================